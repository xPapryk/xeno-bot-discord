const { MessageEmbed, Message } = require("discord.js");

module.exports = {
    name: "test2", 
    category: "test",
    description: "test#2",
    run: async (client, message, args) => {



    }
}
